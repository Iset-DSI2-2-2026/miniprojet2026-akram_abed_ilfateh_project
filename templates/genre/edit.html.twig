{% extends 'base.html.twig' %}

{% block title %}Modifier genre - BookShelf{% endblock %}

{% block body %}
    <div class="card">
        <div class="card-header">
            <h3 class="mb-0">✏️ Modifier genre</h3>
        </div>
        <div class="card-body">
            {{ include('genre/_form.html.twig', {'button_label': 'Mettre à jour'}) }}
        </div>
    </div>
{% endblock %}